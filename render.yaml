services:
  - type: web
    name: tradingview-cornix-webhook
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn advanced_webhook_server:app
    envVars:
      - key: TELEGRAM_BOT_TOKEN
        sync: false
      - key: TELEGRAM_CHAT_ID
        sync: false
